<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
  	<%= link_to 'Clock Management', root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">

        <% if teacher_signed_in? %>
          <li class="nav-item">
  				<%= link_to 'Add Tracking', new_tracking_path, class:"nav-link" %>
        	</li>
        	<li class="nav-item">
  				<%= link_to 'Edit Profile', edit_teacher_registration_path, class:"nav-link" %>
        	</li>
	        <li class="nav-item">
	        </li>
	    <% else %>
	        <li class="nav-item">
	  			<%= link_to 'Sign Up', new_teacher_registration_path, class:"nav-link" %>
	        </li>
	        <li class="nav-item">
	  			<%= link_to 'Log In', new_teacher_session_path, class:"nav-link" %>
	        </li>
	    <% end %>

      </ul>
      <% if teacher_signed_in? %>
	        <span class="card"> <%= current_teacher.email %></span>
	        <span> <%= link_to 'Sign Out', destroy_teacher_session_path, method: :delete, class:"nav-link" %> </span>
	  <% end %>
    </div>
  </div>
</nav>